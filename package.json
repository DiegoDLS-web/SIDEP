{
  "name": "sidep",
  "private": true,
  "description": "SIDEP — despliegue unificado (Angular + API Express)",
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "build": "npm run build:frontend && npm run prisma:generate && npm run build:backend",
    "prisma:generate": "npm run prisma:generate --prefix backend",
    "prisma:migrate:deploy": "npm run prisma:migrate:deploy --prefix backend",
    "start": "npm run start --prefix backend",
    "build:production": "npm run install:all && npm run build && npm run prisma:migrate:deploy"
  },
  "engines": {
    "node": ">=20"
  }
}
